# SOPS age recipients for devbox secret encryption.
#
# Add age public keys for each environment that needs to decrypt secrets:
# - Personal/workstation keys
# - CI keys (via GitHub Actions secrets)
#
# Generate a new key pair: age-keygen -o key.txt
# Add the public key (age1...) from key.txt below.

creation_rules:
  - age:
      # Software Patterns CI
      # - age1... (add your public key here)
